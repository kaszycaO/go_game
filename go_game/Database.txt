CREATE DATABASE gogame;
CREATE TABLE games(
    	id_gry int,
		id_ruchu int,
		X int,
		Y int,
		stan VARCHAR(1)
	 );

	 CREATE USER 'DO'@'localhost' IDENTIFIED BY 'root';
     GRANT ALL PRIVILEGES ON gogame.* TO 'DO'@'localhost';
     FLUSH PRIVILEGES;
