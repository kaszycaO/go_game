package tp.project.go_game;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;

import java.util.ArrayList;
import java.util.Arrays;

import org.junit.Test;

import tp.project.go_game.logic.AppEngine;

public class SuicideTest {
/*
	Integer[] sample;
	
	@Test
	public void coordsAndSuicidalTest() {
		AppEngine engine = new AppEngine(9);
		
		//engine.addStone(1, 0);
		//engine.addStone(1, 1);
		//engine.addStone(0, 1);
		engine.addStone(2, 2);
		engine.changeTurn();
		ArrayList<Integer[]> coords = engine.getCoordsToCheck(1,1);
		ArrayList<Integer[]> expectedCoords = new ArrayList<Integer[]>();
		
		sample= new Integer[2];
		
		//Integer[] sample1 = {0,1};
		//Integer[] sample2 = {2,1};
		//Integer[] sample3 = {1,0};
		//Integer[] sample4 = {1,2};
		//expectedCoords.add(sample1);
		
		sample[0] = 0;
		sample[1] = 1;
		
		expectedCoords.add(sample);
		
		//Arrays.fill(sample, null);
		
		sample[0] = 2;
		sample[1] = 1;
		expectedCoords.add(sample);
		
		//Arrays.fill(sample, null);
		
		sample[0] = 0;
		sample[1] = 1;
		expectedCoords.add(sample);
		//Arrays.fill(sample, null);
		
		
		sample[0] = 1;
		sample[1] = 2;
		expectedCoords.add(sample);
		//Arrays.fill(sample, null);
		//expectedCoords.add(sample3);
		//expectedCoords.add(sample4);
		//System.out.println(expectedCoords.get(0)[0]+ " "+ coords.get(0)[0]);
		//System.out.println(expectedCoords.get(1)[0]+ " "+ coords.get(1)[0]);
		//System.out.println(expectedCoords.get(2)[0]+ " "+ coords.get(2)[0]);
		//System.out.println(expectedCoords.get(3)[0]+ " "+ coords.get(3)[0]);
	
		
		
		for(int i=0;i<4;i++) {
			
			System.out.println(expectedCoords.get(i)[0]+" "+ expectedCoords.get(i)[1]);
		}
	
		for(int i=0;i<4;i++) {
			
			assertEquals(coords.get(i)[0], expectedCoords.get(i)[0]);
			assertEquals(coords.get(i)[1], expectedCoords.get(i)[1]);
		}
		assertTrue(engine.checkIfGotBreaths(1, 1));
		assertFalse(engine.checkIfStrangled(1, 1));
		assertFalse(engine.checkIfSuicidal(1, 1));
	}
	*/
}
